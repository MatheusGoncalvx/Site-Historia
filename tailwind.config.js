tailwind.config = {
  theme: {
    extend: {
      fontFamily: {
        sans: ['Caveat', 'sans-serif'], // Fonte padrão para sans-serif
        lato: ['Lato', 'sans-serif'],  // Adicionando Lato
      },
      boxShadow: {
        'custom-yellow': '0 4px 10px rgba(255, 223, 77, 0.5)', // Sombras personalizadas
      },
    },
  },
};
